/*
 * Оператори порівняння
 */

console.log(15 < 1);

/*
 * Приведення (перетворення) типів до числа
 */

console.log('10' > 5);

/*
 * Оператори рівності (== і ===) та нерівності (!= і !==)
 * ✅ «ЗАВЖДИ ВИКОРИСТОВУЄМО СТРОГО РІВНІСТЬ (===) і НЕРАВЕНСТВО (!==)»
 */

const isEquel = 1 === '1';

console.log(isEquel);